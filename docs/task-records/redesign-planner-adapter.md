# Planner adapter task record

- Owner: isolated Planner connection/domain adapter worker.
- Base: `dc6f23a` (`feat: establish workspace redesign foundation`).
- Scope: only `js/workspace-planner-adapter.js`, focused Planner adapter tests, and this task record.
- Reserved integration: shared entrypoints, manifests, router, UI, backend, deployment, and broad test/build/visual runs remain manager-owned.
- Contract: expose a UI-consumable Planner state and operations for verified Nest gating, capabilities/consent, range loading and filters, personal event create/edit/move/resize/delete, explicit deduplicated import, retained drafts/errors, and timezone-safe view/range helpers.
- Invariants: display profile is never authentication; stale responses, account switches, and permission loss cannot commit state; failed drafts remain; Canvas due dates are immutable and scheduling creates separate personal events; import keeps originals until each successful create; workspace-model v1 records/keys remain unchanged.
- Initial checks: clean worktree on `dc6f23a`; Codex primary usage was 78% at start and 85% after inspection. The manager then reported 91%, crossing the user-mandated hard stop.
- Completed inspection: read `PRODUCT.md`, redesign plan/Foundation record, `workspace-foundation.js`, the feature host/context in `edit-canvas.js`, workspace-model v1, bridge contract/router range and personal mutation paths, transport calendar sanitization, and the read-only Nest extension calendar/identity/consent handlers.
- Confirmed bridge contract: range reads use `NEST_CALENDAR_RANGE_GET` with exact ISO `start`/`end`; personal writes use `NEST_CALENDAR_EVENT_CREATE`, `NEST_CALENDAR_EVENT_UPDATE`, and `NEST_CALENDAR_EVENT_DELETE`. The router maps these only to `/api/extension/calendar/events`, rechecks sender/binding/verified Nest identity/current consent/capabilities, and supplies `X-Canvas-Account-Key`. The adapter must call this bridge and must not add a sync protocol.
- Confirmed UI-facing design: fail-closed read/write access states; immutable snapshots for loading/range/events/sources/filters/drafts/import/error; stale response token bound to current Canvas account key and verified Nest identity; failed mutation drafts retained; explicit import with source-id deduplication and per-item success/failure while returning originals; Sunday day/week/month range helpers using timezone-local midnight, 15-minute snapping, and one-hour default timed duration.
- Results: no implementation or test file was created. A combined source/test patch failed to parse before applying, so there is no partial production code to preserve. Per the usage instruction, no retry or test run was attempted.
- Blockers/integration seams: Foundation exposes `context.account.scope` as `canvas:<sha256 accountKey>` and the Canvas origin, but workspace-model v1 requires the original verified numeric `accountId` together with the origin. The adapter must not derive or invent that identifier. Manager integration must expose the verified legacy `accountId` (or an already-created workspace-model store) to preserve `apstudycanvas.workspace.v1:<origin>:<accountId>` without orphaning existing records. Also ensure the UI supplies live capability/consent refreshes; display profile data remains non-authoritative.
- Checks at checkpoint: no tests/build/full suite/visual/live mutation run, as required by the hard stop. Only this force-added task record is changed.
- Next action: after explicit renewed instruction and available usage, create `js/workspace-planner-adapter.js` and `tests/extension/workspace-planner-adapter.test.js` from the confirmed contract; test gating, DST/Sunday ranges, stale account/permission responses, retained failed drafts, personal-only mutation families, and explicit deduplicated import. Shared wiring remains manager-owned.
